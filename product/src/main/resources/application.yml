spring:
  application:
    name: product-service
  cloud:
    consul:
      host: consul
      port: 8500

server:
  port: 8903

---
spring:
  profiles: dev
  application:
    name: product-service
  h2:
    console:
      enabled: true
  cloud:
    consul:
      host: localhost
      port: 8500

server:
  port: 8903
